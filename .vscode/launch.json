{
    "configurations": [
        {
            "command": "npm run dev",
            "name": "Run npm dev",
            "request": "launch",
            "type": "node-terminal",
            "env": {
                "VITE_API_URL": "http://localhost:8000"
            }
        }
    ]
}